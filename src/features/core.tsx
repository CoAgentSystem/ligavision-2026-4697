{
    "deliverable": {
        "title": "Real-time Match Visualization Engine",
        "description": "Core feature that processes live soccer match data and renders interactive 3D visualizations of player movements, formations, and tactical patterns using Three.js and WebGL.",
        "implementation": {
            "files": [
                {
                    "path": "src/core/visualization/MatchVisualizer.ts",
                    "content": "import * as THREE from 'three';\nimport { OrbitControls } from 'three/examples/jsm/controls/OrbitControls';\nimport { MatchData, PlayerPosition, TeamFormation } from '../types/match.types';\n\n/**\n * Real-time Match Visualization Engine\n * Core differentiating feature for ligavision-2026-4697\n * Renders interactive 3D soccer match visualizations with player tracking\n */\nexport class MatchVisualizer {\n  private scene: THREE.Scene;\n  private camera: THREE.PerspectiveCamera;\n  private renderer: THREE.WebGLRenderer;\n  private controls: OrbitControls;\n  private playerMeshes: Map<string, THREE.Mesh> = new Map();\n  private pitchMesh: THREE.Mesh;\n  private animationId: number | null = null;\n  \n  constructor(container: HTMLElement) {\n    // Initialize Three.js scene\n    this.scene = new THREE.Scene();\n    this.scene.background = new THREE.Color(0x1a1a2e);\n    \n    // Set up camera (overhead view of pitch)\n    this.camera = new THREE.PerspectiveCamera(\n      60,\n      container.clientWidth / container.clientHeight,\n      0.1,\n      1000\n    );\n    this.camera.position.set(0, 50, 50);\n    this.camera.lookAt(0, 0, 0);\n    \n    // Initialize WebGL renderer\n    this.renderer = new THREE.WebGLRenderer({ antialias: true });\n    this.renderer.setSize(container.clientWidth, container.clientHeight);\n    this.renderer.setPixelRatio(window.devicePixelRatio);\n    container.appendChild(this.renderer.domElement);\n    \n    // Add orbit controls for interactive viewing\n    this.controls = new OrbitControls(this.camera, this.renderer.domElement);\n    this.controls.enableDamping = true;\n    this.controls.dampingFactor = 0.05;\n    \n    // Add lighting\n    const ambientLight = new THREE.AmbientLight(0xffffff, 0.6);\n    this.scene.add(ambientLight);\n    \n    const directionalLight = new THREE.DirectionalLight(0xffffff, 0.8);\n    directionalLight.position.set(50, 100, 50);\n    this.scene.add(directionalLight);\n    \n    // Create soccer pitch\n    this.createPitch();\n    \n    // Handle window resize\n    window.addEventListener('resize', () => this.onWindowResize(container));\n    \n    // Start animation loop\n    this.animate();\n  }\n  \n  /**\n   * Creates a 3D soccer pitch with markings\n   */\n  private createPitch(): void {\n    // Pitch dimensions (meters)\n    const pitchWidth = 68;\n    const pitchLength = 105;\n    \n    // Pitch surface\n    const pitchGeometry = new THREE.PlaneGeometry(pitchLength, pitchWidth);\n    const pitchMaterial = new THREE.MeshPhongMaterial({\n      color: 0x2d5016,\n      side: THREE.DoubleSide\n    });\n    this.pitchMesh = new THREE.Mesh(pitchGeometry, pitchMaterial);\n    this.pitchMesh.rotation.x = Math.PI / 2;\n    this.scene.add(this.pitchMesh);\n    \n    // Pitch markings (white lines)\n    const lineMaterial = new THREE.LineBasicMaterial({ color: 0xffffff });\n    \n    // Outer boundary\n    const boundaryPoints = [\n      new THREE.Vector3(-pitchLength/2, 0.01, -pitchWidth/2),\n      new THREE.Vector3(pitchLength/2, 0.01, -pitchWidth/2),\n      new THREE.Vector3(pitchLength/2, 0.01, pitchWidth/2),\n      new THREE.Vector3(-pitchLength/2, 0.01, pitchWidth/2),\n      new THREE.Vector3(-pitchLength/2, 0.01, -pitchWidth/2)\n    ];\n    const boundaryGeometry = new THREE.BufferGeometry().setFromPoints(boundaryPoints);\n    const boundaryLine = new THREE.Line(boundaryGeometry, lineMaterial);\n    this.scene.add(boundaryLine);\n    \n    // Center circle\n    const centerCircleGeometry = new THREE.RingGeometry(9.15, 9.2, 32);\n    const centerCircleMaterial = new THREE.MeshBasicMaterial({\n      color: 0xffffff,\n      side: THREE.DoubleSide\n    });\n    const centerCircle = new THREE.Mesh(centerCircleGeometry, centerCircleMaterial);\n    centerCircle.rotation.x = Math.PI / 2;\n    centerCircle.position.y = 0.01;\n    this.scene.add(centerCircle);\n    \n    // Center line\n    const centerLineGeometry = new THREE.BufferGeometry().setFromPoints([\n      new THREE.Vector3(0, 0.01, -pitchWidth/2),\n      new THREE.Vector3(0, 0.01, pitchWidth/2)\n    ]);\n    const centerLine = new THREE.Line(centerLineGeometry, lineMaterial);\n    this.scene.add(centerLine);\n  }\n  \n  /**\n   * Updates visualization with new match data\n   * @param matchData Real-time match data including player positions\n   */\n  public updateMatchData(matchData: MatchData): void {\n    // Clear previous player meshes\n    this.playerMeshes.forEach(mesh => this.scene.remove(mesh));\n    this.playerMeshes.clear();\n    \n    // Create player markers for both teams\n    matchData.players.forEach(player => {\n      const playerMesh = this.createPlayerMarker(player);\n      this.playerMeshes.set(player.id, playerMesh);\n      this.scene.add(playerMesh);\n    });\n    \n    // Update player positions\n    this.updatePlayerPositions(matchData);\n    \n    // Visualize team formations\n    this.visualizeFormations(matchData.homeFormation, matchData.awayFormation);\n  }\n  \n  /**\n   * Creates a 3D marker for a player\n   */\n  private createPlayerMarker(player: PlayerPosition): THREE.Mesh {\n    const geometry = new THREE.SphereGeometry(0.8, 16, 16);\n    const material = new THREE.MeshPhongMaterial({\n      color: player.team === 'home' ? 0xff6b6b : 0x4ecdc4,\n      emissive: player.team === 'home' ? 0xff6b6b : 0x4ecdc4,\n      emissiveIntensity: 0.2\n    });\n    \n    const mesh = new THREE.Mesh(geometry, material);\n    mesh.position.set(\n      player.position.x - 52.5, // Convert to pitch coordinates\n      1, // Slightly above pitch\n      player.position.y - 34\n    );\n    \n    // Add player number label\n    const canvas = document.createElement('canvas');\n    canvas.width = 64;\n    canvas.height = 64;\n    const context = canvas.getContext('2d');\n    if (context) {\n      context.fillStyle = '#ffffff';\n      context.font = 'bold 48px Arial';\n      context.textAlign = 'center';\n      context.textBaseline = 'middle';\n      context.fillText(player.number.toString(), 32, 32);\n      \n      const texture = new THREE.CanvasTexture(canvas);\n      const spriteMaterial = new THREE.SpriteMaterial({ map: texture });\n      const sprite = new THREE.Sprite(spriteMaterial);\n      sprite.scale.set(4, 4, 1);\n      sprite.position.y = 2;\n      mesh.add(sprite);\n    }\n    \n    return mesh;\n  }\n  \n  /**\n   * Updates positions of all player markers\n   */\n  private updatePlayerPositions(matchData: MatchData): void {\n    matchData.players.forEach(player => {\n      const mesh = this.playerMeshes.get(player.id);\n      if (mesh) {\n        mesh.position.x = player.position.x - 52.5;\n        mesh.position.z = player.position.y - 34;\n        \n        // Add subtle animation for movement\n        mesh.position.y = 1 + Math.sin(Date.now() * 0.01) * 0.1;\n      }\n    });\n  }\n  \n  /**\n   * Visualizes team formations with connecting lines\n   */\n  private visualizeFormations(homeFormation: TeamFormation, awayFormation: TeamFormation): void {\n    // Create formation visualization for home team\n    this.createFormationLines(homeFormation, 0xff6b6b);\n    \n    // Create formation visualization for away team\n    this.createFormationLines(awayFormation, 0x4ecdc4);\n  }\n  \n  /**\n   * Creates connecting lines between players in a formation\n   */\n  private createFormationLines(formation: TeamFormation, color: number): void {\n    const lineMaterial = new THREE.LineBasicMaterial({\n      color: color,\n      opacity: 0.3,\n      transparent: true\n    });\n    \n    // Create lines between adjacent players in formation\n    formation.positions.forEach((pos1, i) => {\n      formation.positions.slice(i + 1).forEach(pos2 => {\n        const distance = Math.sqrt(\n          Math.pow(pos2.x - pos1.x, 2) + Math.pow(pos2.y - pos1.y, 2)\n        );\n        \n        // Only connect players within reasonable distance\n        if (distance < 30) {\n          const points = [\n            new THREE.Vector3(pos1.x - 52.5, 0.5, pos1.y - 34),\n            new THREE.Vector3(pos2.x - 52.5, 0.5, pos2.y - 34)\n          ];\n          \n          const geometry = new THREE.BufferGeometry().setFromPoints(points);\n          const line = new THREE.Line(geometry, lineMaterial);\n          this.scene.add(line);\n        }\n      });\n    });\n  }\n  \n  /**\n   * Animation loop\n   */\n  private animate(): void {\n    this.animationId = requestAnimationFrame(() => this.animate());\n    \n    // Update controls\n    this.controls.update();\n    \n    // Render scene\n    this.renderer.render(this.scene, this.camera);\n  }\n  \n  /**\n   * Handles window resize\n   */\n  private onWindowResize(container: HTMLElement): void {\n    this.camera.aspect = container.clientWidth / container.clientHeight;\n    this.camera.updateProjectionMatrix();\n    this.renderer.setSize(container.clientWidth, container.clientHeight);\n  }\n  \n  /**\n   * Clean up resources\n   */\n  public dispose(): void {\n    if (this.animationId) {\n      cancelAnimationFrame(this.animationId);\n    }\n    \n    this.renderer.dispose();\n    this.playerMeshes.clear();\n    \n    // Remove event listeners\n    window.removeEventListener('resize', () => this.onWindowResize);\n  }\n}"
                },
                {
                    "path": "src/core/types/match.types.ts",
                    "content": "/**\n * Type definitions for match data\n */\n\nexport interface PlayerPosition {\n  id: string;\n  name: string;\n  number: number;\n  team: 'home' | 'away';\n  position: {\n    x: number; // 0-105 (pitch length)\n    y: number; // 0-68 (pitch width)\n  };\n  speed: number;\n  direction: number;\n}\n\nexport interface TeamFormation {\n  name: string;\n  positions: Array<{\n    x: number;\n    y: number;\n    role: string;\n  }>;\n}\n\nexport interface MatchData {\n  matchId: string;\n  timestamp: number;\n  players: PlayerPosition[];\n  homeFormation: TeamFormation;\n  awayFormation: TeamFormation;\n  ballPosition: {\n    x: number;\n    y: number;\n    z: number;\n  };\n  score: {\n    home: number;\n    away: number;\n  };\n}"
                },
                {
                    "path": "src/core/visualization/MatchVisualizerDemo.tsx",
                    "content": "import React, { useEffect, useRef } from 'react';\nimport { MatchVisualizer } from './MatchVisualizer';\nimport { MatchData } from '../types/match.types';\nimport './MatchVisualizerDemo.css';\n\n/**\n * Demo component showing how to integrate MatchVisualizer into React app\n */\nconst MatchVisualizerDemo: React.FC = () => {\n  const containerRef = useRef<HTMLDivElement>(null);\n  const visualizerRef = useRef<MatchVisualizer | null>(null);\n  \n  useEffect(() => {\n    if (!containerRef.current) return;\n    \n    // Initialize visualizer\n    visualizerRef.current = new MatchVisualizer(containerRef.current);\n    \n    // Simulate real-time match data updates\n    const interval = setInterval(() => {\n      const mockData = generateMockMatchData();\n      visualizerRef.current?.updateMatchData(mockData);\n    }, 100); // Update 10 times per second\n    \n    return () => {\n      clearInterval(interval);\n      visualizerRef.current?.dispose();\n    };\n  }, []);\n  \n  return (\n    <div className=\"match-visualizer-demo\">\n      <div className=\"visualizer-container\" ref={containerRef} />\n      <div className=\"controls\">\n        <h3>Real-time Match Visualization</h3>\n        <p>Interactive 3D view of player positions and formations</p>\n        <div className=\"legend\">\n          <div className=\"legend-item\">\n            <span className=\"color home\" />\n            <span>Home Team</span>\n          </div>\n          <div className=\"legend-item\">\n            <span className=\"color away\" />\n            <span>Away Team</span>\n          </div>\n        </div>\n      </div>\n    </div>\n  );\n};\n\n/**\n * Generates mock match data for demonstration\n */\nfunction generateMockMatchData(): MatchData {\n  const players: PlayerPosition[] = [];\n  \n  // Generate home team players (4-3-3 formation)\n  const homeFormation = {\n    name: '4-3-3',\n    positions: [\n      { x: 5, y: 34, role: 'GK' },\n      { x: 20, y: 10, role: 'RB' },\n      { x: 20, y: 25, role: 'CB' },\n      { x: 20, y: 43, role: 'CB' },\n      { x: 20, y: 58, role: 'LB' },\n      { x: 35, y: 20, role: 'CM' },\n      { x: 35, y: 34, role: 'CM' },\n      { x: 35, y: 48, role: 'CM' },\n      { x: 60, y: 15, role: 'RW' },\n      { x: 60, y: 34, role: 'ST' },\n      { x: 60, y: 53, role: 'LW' }\n    ]\n  };\n  \n  // Generate away team players (4-4-2 formation)\n  const awayFormation = {\n    name: '4-4-2',\n    positions: [\n      { x: 100, y: 34, role: 'GK' },\n      { x: 85, y: 10, role: 'RB' },\n      { x: 85, y: 25, role: 'CB' },\n      { x: 85, y: 43, role: 'CB' },\n      { x: 85, y: 58, role: 'LB' },\n      { x: 70, y: 15, role: 'RM' },\n      { x: 70, y: 30, role: 'CM' },\n      { x: 70, y: 38, role: 'CM' },\n      { x: 70, y: 53,